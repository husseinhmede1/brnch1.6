package com.mdsl.utils.specifications;

public final class AppHelpTopicSpecifications {
	/*
	 * public static Specification<AppHelpTopic> topicContains(String topic) {
	 * return (root, query, builder) ->
	 * builder.like(root.get("topicSearchKeywords"), contains(topic)); }
	 * 
	 * private static String contains(String expression) { return
	 * MessageFormat.format("%{0}%", expression.toLowerCase()); }
	 * 
	 * public static Specification<AppHelpTopic> appInfoEquals (AppInfo appInfo) {
	 * return (root, query, builder) -> builder.equal(root.get("appInfo"), appInfo);
	 * }
	 */}